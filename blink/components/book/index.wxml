<view bind:tap="onTap" class="container">
    <image src="{{book.image}}" />
    <view class="description">
        <text class="title">{{book.title}}</text>
        <text class="author">{{book.author}}</text>
        <view class="foot">
            <text class="footer">{{book.fav_nums}}  like</text>
        </view>
    </view>
</view>